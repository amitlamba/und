spring:
  profiles:
    active: dev
  cloud:
    config:
      uri: http://nestros.com:8888
      enabled: true
      name: event-api
  application:
    name: event-api
  mvc:
    view:
      prefix:

