spring:
  profiles:
    active:
      shiv
  cloud:
    config:
      uri: http://nestros.com:8888
      enabled: true
      name: auth-service
  application:
    name: auth-service
  mvc:
    view:
      prefix: