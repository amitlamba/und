spring:
  cloud:
    config:
      uri: http://${CONFIG_IP}:${CONFIG_PORT}
      enabled: true
      name: emailsendservice
      profile: staging
  application:
    name: emailsendservice

encrypt:
  key: ${ENCRYPT_KEY}