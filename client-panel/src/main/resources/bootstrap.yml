spring:
  cloud:
    config:
      uri: ${CONFIG_IP}:${CONFIG_PORT}
      enabled: true
      name: client-panel
      profile: staging
  application:
    name: client-panel

encrypt:
  key: ${ENCRYPT_KEY}
#  fail-on-error: false